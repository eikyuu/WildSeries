{% extends 'base.html.twig' %}

{% block body %}
    <h1>All TV shows from "{{ categoryName }}" category: </h1>
    {% for program in programs %}
        <div>
        <a href="{{ path('wild_show', { 'slug': "the-walking-dead" }) }}">{{ program.title }}</a>
            <h2>{{ loop.index }} / {{ program.title }}</h2>
            <p>{{ program.synopsis }}</p>
        </div>
    {% endfor %}
{% endblock %}

            